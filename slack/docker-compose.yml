# Slack Bot Integration for CX Agent Studio
# Docker Compose configuration

version: '3.8'

services:
  slack-ces-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: slack-ces-bot
    restart: unless-stopped

    # Environment variables
    environment:
      - SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN}
      - SLACK_APP_TOKEN=${SLACK_APP_TOKEN}
      - SLACK_SIGNING_SECRET=${SLACK_SIGNING_SECRET}
      - GCP_PROJECT_ID=${GCP_PROJECT_ID}
      - GCP_REGION=${GCP_REGION:-us}
      - CES_APP_ID=${CES_APP_ID}
      - CES_DEPLOYMENT_ID=${CES_DEPLOYMENT_ID:-}
      - USE_SOCKET_MODE=${USE_SOCKET_MODE:-true}
      - RESPOND_TO_MENTIONS_ONLY=${RESPOND_TO_MENTIONS_ONLY:-false}
      - USE_BIDI_SESSION=${USE_BIDI_SESSION:-false}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - HEALTH_CHECK_PORT=${HEALTH_CHECK_PORT:-8080}
      - GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/service-account-key.json

    # Mount service account credentials
    volumes:
      - ${GOOGLE_APPLICATION_CREDENTIALS:-./service-account-key.json}:/app/credentials/service-account-key.json:ro

    # Expose ports
    ports:
      - "${HEALTH_CHECK_PORT:-8080}:8080"
      - "3000:3000"  # Events API port (if not using Socket Mode)

    # Resource limits
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.1'
          memory: 128M

    # Health check
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

    # Logging configuration
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

# Network configuration (optional)
networks:
  default:
    name: slack-ces-network
